RSpec.describe Active::Elastic::Job2 do
  it "has a version number" do
    expect(Active::Elastic::Job2::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
